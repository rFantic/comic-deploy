apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: comic-prod

secretGenerator:
- name: backend-env
  envs:
  - .env
  type: Opaque

resources:
- database.yaml
- backend.yaml
- frontend.yaml
- istio-resources.yaml
