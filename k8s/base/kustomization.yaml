apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: comic-prod

resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - minio.yaml
  - backend.yaml
  - worker.yaml
  - frontend.yaml
  - istio.yaml
  - cloudflared.yaml

secretGenerator:
  - name: comic-secrets
    envs:
      - .env.shared
